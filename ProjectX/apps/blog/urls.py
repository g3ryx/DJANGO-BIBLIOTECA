from django.urls import path
from .views import *

app_name = 'blog_app'

urlpatterns = [
 #   path('', ListPosts.as_view(), name="list-posts"),
  #  path('post/<int:pk>', PostView.as_view(), name="single-post"),
]
